language: generic

sudo: required
services:
  - docker

before_install:
  - docker build -t jhearington/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true jhearington/react-test npm test

after_success:
  - docker build -t brownie/multi-client ./client
  - docker build -t brownie/multi-nginx ./multi-nginx
  - docker built -t brownie/multi-server ./server
  - docker built -t brownie/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PW" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push brownie/multi-client
  - docker push brownie/multi-nginx
  - docker push brownie/multi-server
  - docker push brownie/multi-worker
